# ─── Environment ──────────────────────────────────────────────
NODE_ENV=development          # development | test | production
PORT=3001                     # HTTP listen port

# ─── Database (required outside test) ────────────────────────
MONGO_URI=mongodb://localhost:27017/swiftly

# ─── JWT (required outside test) ─────────────────────────────
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES=12h
JWT_REFRESH_SECRET=change-me-another-long-random-string   # falls back to JWT_SECRET if omitted
JWT_REFRESH_EXPIRES=7d

# ─── CORS ────────────────────────────────────────────────────
CORS_ORIGIN=http://localhost:5173   # comma-separated origins

# ─── Logging ─────────────────────────────────────────────────
LOG_LEVEL=info                # fatal | error | warn | info | debug | trace | silent

# ─── Request limits ──────────────────────────────────────────
REQUEST_BODY_LIMIT=1mb

# ─── OpenAI (optional — AI features disabled without it) ─────
# OPENAI_API_KEY=sk-...

# ─── Redis (optional — AI conversation memory disabled) ──────
# REDIS_URL=redis://localhost:6379
# AI_CONVERSATION_TTL_MS=1800000   # 30 min

# ─── Cloudinary (required outside test) ──────────────────────
# Option A: single URL
# CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
# Option B: separate values
# CLOUD_NAME=your-cloud-name
# CLOUD_API_KEY=your-api-key
# CLOUD_API_SECRET=your-api-secret

# ─── Auth cookies (optional overrides) ───────────────────────
# AUTH_COOKIE_SECURE=true
# AUTH_COOKIE_SAMESITE=lax
# AUTH_COOKIE_DOMAIN=.example.com
# AUTH_ACCESS_COOKIE_NAME=__Host-swiftly_helpdesk_access
# AUTH_REFRESH_COOKIE_NAME=__Host-swiftly_helpdesk_refresh
